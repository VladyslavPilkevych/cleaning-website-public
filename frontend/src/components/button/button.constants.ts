export enum ButtonVariant {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  ACCENT = "accent",
  DARK = "dark",
  LIGHT = "light",
  WARNING = "warning",
  HIGHLIGHT = "highlight",
}

export enum ButtonSize {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}

export enum ButtonType {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}
